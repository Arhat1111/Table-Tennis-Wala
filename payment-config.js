// InkWaves payment frontend configuration.
// Leave blank when the website and Node backend use the same domain.
// If your storefront is static and your backend is hosted elsewhere, paste the backend origin here,
// for example: 'https://inkwaves-api.example.com'
window.INKWAVES_PAYMENT_API_BASE = '';

// Admin/catalog sync uses the same backend by default.
window.INKWAVES_ADMIN_API_BASE = window.INKWAVES_PAYMENT_API_BASE;
